#include <iostream>
#include <math.h>
#include <cmath>
using namespace std;

int main(){
    double x=5;
    double y=3;
    double result;

    //result=max(x,y);
    //result=min(x,y);
    //result=pow(x,y);
    //result=sqrt(9);
    //result=floor(2.7); //rounds down to nearest integer
    //result=ceil(2.7); //rounds up to nearest integer
    //result=abs(-10);
    //cout<<result;



    double a=3,b=4,c;
    c=sqrt(pow(a,2)+pow(b,2));
    cout<<"Hypotensuse is: "<<c;
}